# Graph Neural Network stack — required only for ml/gnn_ring_scorer.py
# If installation fails (e.g. no matching CUDA wheel on this machine), comment
# these two lines out. The rest of AEGIS (baseline XGBoost classifier, Exasol
# views, Bayesian explainability) works fully without them — see ml/gnn_ring_scorer.py
# which falls back to the networkx heuristic score if torch_geometric is absent.
torch==2.3.1
torch-geometric==2.5.3
