fastapi==0.111.0
uvicorn[standard]==0.30.1
pyexasol==0.27.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
xgboost==2.0.3
networkx==3.3
pgmpy==0.1.25
python-dotenv==1.0.1
pydantic==2.7.4
faker==25.8.0
shap>=0.40.0

# Optional GPU / graph deep-learning stack (installed if CUDA base image is used).
# Left as separate optional file so the baseline (steps 1-4 classifier) always
# builds even on CPU-only machines.
-r requirements-gnn.txt
